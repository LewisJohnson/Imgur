<controls:MetroWindow x:Class="ImgurUploader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      Title="MainWindow" Height="998.24" Width="635.125">
    <Grid>
        <Grid HorizontalAlignment="Left" Height="947" VerticalAlignment="Top" Width="607" Margin="10,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="146*"/>
                <ColumnDefinition Width="297*"/>
                <ColumnDefinition Width="164*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="131*"/>
                <RowDefinition Height="48*"/>
                <RowDefinition Height="42*"/>
                <RowDefinition Height="44*"/>
                <RowDefinition Height="194*"/>
                <RowDefinition Height="488*"/>
            </Grid.RowDefinitions>
            <Canvas HorizontalAlignment="Center"  Height="133" VerticalAlignment="Top" Width="270" Grid.Column="1" Margin="15,0,12,0" Grid.RowSpan="2">
                <Canvas.Background>
                    <ImageBrush ImageSource="Resources/imgur_logo.png" Stretch="Uniform"/>
                </Canvas.Background>
            </Canvas>
            <Button x:Name="ImageSelector" Content="Select" Margin="10,10,25,0" Grid.Row="2" VerticalAlignment="Top" Click="ImageSelector_OnClick" Height="25"/>
            <Button x:Name="Upload" Content="Upload" Margin="10,10,25,0" Grid.Row="3" VerticalAlignment="Top" Click="Upload_OnClick" Height="25"/>
            <Label x:Name="Label" Content="Response:" HorizontalAlignment="Left" Margin="64,153,0,0" Grid.Row="5"
                   Grid.Column="0" VerticalAlignment="Top" Height="26" Width="64"/>
            <TextBlock x:Name="Link"  HorizontalAlignment="Left" Text="{Binding linkString, Mode=OneWay}" TextWrapping="Wrap" VerticalAlignment="Top" Grid.Row="5" RenderTransformOrigin="0.297,0.595" Width="444" Height="325" Grid.ColumnSpan="2" Grid.Column="1" Margin="0,153,0,0"/>
            <Label x:Name="label" Content="Anonymous upload" HorizontalAlignment="Left" Height="23" Margin="10,10,0,0" VerticalAlignment="Top" Width="118" Grid.Row="1"/>

        </Grid>

    </Grid>
    <controls:MetroWindow.IconTemplate>
        <DataTemplate>
            <Grid Width="{TemplateBinding Width}"
                 Height="{TemplateBinding Height}"
                 Margin="4"
                 Background="Transparent"
                 RenderOptions.EdgeMode="Aliased"
                 RenderOptions.BitmapScalingMode="HighQuality">
                <Rectangle>
                    <Rectangle.Fill>
                        <VisualBrush Visual="{StaticResource appbar_alien}" />
                    </Rectangle.Fill>
                </Rectangle>
            </Grid>
        </DataTemplate>
    </controls:MetroWindow.IconTemplate>
</controls:MetroWindow>
