<controls:MetroWindow x:Class="ImgurUploader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      Title="MainWindow" Height="417.111" Width="635.125">
    <Grid>
        <Grid HorizontalAlignment="Left" Height="366" VerticalAlignment="Top" Width="607" Margin="10,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130*"/>
                <ColumnDefinition Width="247*"/>
                <ColumnDefinition Width="140*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="125*"/>
                <RowDefinition Height="81*"/>
                <RowDefinition Height="32*"/>
                <RowDefinition Height="81*"/>
            </Grid.RowDefinitions>
            <Canvas HorizontalAlignment="Left"  Height="133" VerticalAlignment="Top" Width="270" Grid.Column="1" Margin="10,0,0,0">
                <Canvas.Background>
                    <ImageBrush ImageSource="Resources/imgur_logo.png" Stretch="Uniform"/>
                </Canvas.Background>
            </Canvas>
            <Button x:Name="ImageSelector" Content="Select" Grid.Column="1" Margin="86,36,86,0" Grid.Row="1" VerticalAlignment="Top" Click="ImageSelector_OnClick"/>
            <Button x:Name="Upload" Content="Upload" Grid.Column="1" Margin="86,10,86,0" Grid.Row="2" VerticalAlignment="Top" Click="Upload_OnClick"/>
            <Label x:Name="Label" Content="Link:" HorizontalAlignment="Left" Margin="10,30,0,0" Grid.Row="3"
                   Grid.Column="0" VerticalAlignment="Top"/>
            <ScrollViewer x:Name="Link" Content="" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="44,30,0,0" Grid.Row="3" RenderTransformOrigin="0.297,0.595" Width="389" Height="53" Grid.ColumnSpan="2"
                          Grid.Column="0" />

        </Grid>

    </Grid>
    <controls:MetroWindow.IconTemplate>
        <DataTemplate>
            <Grid Width="{TemplateBinding Width}"
                 Height="{TemplateBinding Height}"
                 Margin="4"
                 Background="Transparent"
                 RenderOptions.EdgeMode="Aliased"
                 RenderOptions.BitmapScalingMode="HighQuality">
                <Rectangle>
                    <Rectangle.Fill>
                        <VisualBrush Visual="{StaticResource appbar_alien}" />
                    </Rectangle.Fill>
                </Rectangle>
            </Grid>
        </DataTemplate>
    </controls:MetroWindow.IconTemplate>
</controls:MetroWindow>
